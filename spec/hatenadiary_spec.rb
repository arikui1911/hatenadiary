require 'spec_helper'

describe Hatenadiary do
  it 'has a version number' do
    expect(Hatenadiary::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
